/**
 * SnippetProvider — Re-exports snippet logic from CompletionProvider.
 * Snippets are integrated into the CompletionProvider for simplicity.
 * This module exists for organizational purposes.
 */

// Snippets are handled by CompletionProvider.
// See providers/CompletionProvider.ts for ::preset, :AddService, :AddController snippets.

export {};
